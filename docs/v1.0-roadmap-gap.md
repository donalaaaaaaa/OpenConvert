# OpenConvert 1.0 后续开发计划书 ↔ 代码现状 Gap

依据：《OpenConvert 1.0 后续开发计划书》§十六 建议开发顺序。
基线：`b879702` + 当前工作树（2026-08-21），JDK17 + SDK36，OnePlus PHY110 / Android 16。

## 阶段对照

| # | 计划书阶段 | 现状 | 真实缺口 |
|---|-----------|------|---------|
| 01 | 稳定性测试体系 | `TestAssetFactory` + 真机异常输入用例已覆盖空文件、截断、损坏主体、内容/扩展名不一致、输出失效，且验证坏任务后好任务仍可运行 | 仍缺 100 MB–4 GB 梯度的真实长时间测试、系统杀进程矩阵及 Android 12–15 / 多 SoC 覆盖 |
| 02 | ConversionPlanner | 能力、编码模式、硬件事实、空间预算、并发槽位已经统一；生产执行器会先探测真实编码并把同一份 `StreamCodecs` 与 `ConversionPlan` 交给 MediaConverter | 已有 MOV(MPEG4+AAC)→MP4 真机生产路径证明 `streamCopy=true`；仍需补 HEVC→H264 硬编失败兜底的端到端断言 |
| 03 | 首页 UI 2.0 | 已有文件驱动入口、能力面板与真机导航回归 | 主流程完成；后续属于视觉与易用性迭代 |
| 04 | 任务中心 2.0 | 已按运行/等待/暂停/失败/完成分组，展示估算速度、剩余时间、完成体积/耗时、结构化错误与实际执行引擎；fallback 结果从 Converter 贯通到 Room v7、任务卡、历史和 Benchmark | 速度仍是基于进度比例的统一估算，不是真实字节计数 |
| 05 | Preset 系统 | Room v7 持久化、12 个内置预设、自定义/默认/删除、单文件应用、尺寸约束与批量应用均已接线 | 批量页目前只允许应用已有预设，不在批量草稿上新建预设（单文件配置页可新建） |
| 06 | PDF 压缩 + 页面排序 | 已完成：`PdfCompressConverter`（三档 DPI）、`PdfPageManager`（缩略图 + 重排 + 旋转 + 删除导出）、加密/解密、裁剪、元数据 | 计划书 P1/P2 基本已落地；P3 水印未做 |
| 07 | Office 兼容优化 | 已新增可复现的 DOCX/PPTX/XLSX 保真度素材矩阵，覆盖中文、复杂/合并表格、页眉页脚、多页/多 Sheet 与公式结果；真机 PDF 页数、文本标记和实际引擎自动断言通过（`docs/phase10-office-fidelity.md`） | 首批内容级门禁已落地；仍缺图表/嵌入对象/受保护文档、像素级 golden diff 与 Android 12–15 / 多 SoC 字体差异覆盖 |
| 08 | Benchmark | `BenchmarkRecord` + JSONL 采集已接入 SINGLE 生产路径；支持 512 KB 轮转、并发安全、周期峰值内存采样；设置页可查看记录数，并通过 SAF 导出 Markdown 聚合报告或带 BOM 的 CSV 明细 | CPU 不采集（Android 只能可靠得到整进程值，无法按任务归因）；工具类多输入任务暂不采集，尚缺串行隔离 runner |
| 09 | APK/AAB 体积治理 | Basic / Office 双 Flavor 已落地并按运行时能力降级；Basic APK 35.39 MiB、PHY110 下载 34.74 MiB、即时 code 107.23 MiB，相比 Office 分别减少 66.39%、66.46%、69.71%；同签名、同 versionCode 的 Basic → Office 原位覆盖已真机验证历史、预设、真实 SAF grant 保留，升级后 Release 三格式转换通过（`docs/phase11-edition-upgrade.md`） | 双 Edition 发布链已闭环；若走商店按需交付应采用 dynamic feature/split APK，Basic 剩余体积继续看 FFmpegKit 裁剪 |
| 10 | Release 1.0 | README 已补截图、架构图、Benchmark、双 Edition 与构建说明；`docs/install.md` + `docs/known-issues.md` + `LICENSE` 已齐；Basic/Office 签名 APK+AAB 已校验并放到 `output/release-1.0/`（`docs/phase12-release.md`） | GitHub `v1.0.0` 仍是 2026-08-15 单体包；双 Edition 资产要等阶段 07–11 工作树提交后再挂 Release |

## 阶段外发现的既有缺陷

1. ~~**`canConvertLocallyTo` 与 `ConversionGraph` 双轨**。~~ → ✅ 已统一到 `ConversionGraph`，并用全格式对回归测试守护。

2. ~~**Graph 声明了无引擎的边**。~~ → ✅ 转换边与工具边已分离，并逐边断言 registry 有执行引擎。

3. ~~**结构化错误未进入 UI**。~~ → ✅ Planner 拒绝、`ConversionException` 与存量字符串均由 `ErrorPresenter` 输出标题、明细与建议；仍应逐步减少 Converter 内部裸字符串。

4. **仓库体积**：`.git` 1.7 GB。**已核实：LFS 早已配置**（`.gitattributes` 有
   `*.so filter=lfs`），`liblo-native-code.so` 在 git 里是 134 字节指针，v1.0.0
   已成功推上 origin。不需要重写历史。实际约束是 GitHub LFS 免费额度
   （1 GB 存储，当前 893 MB）。

5. ~~**Release 签名口令硬编码**在 `app/build.gradle.kts:26`。~~ → ✅ 已外置到
   gitignore 的 `signing.properties` + 环境变量，缺失时产出 unsigned APK。
   注意该 keystore 口令已进历史，视为泄露，正式发布需换新 keystore。

6. ~~旧 `ConversionEngineSelector` 死代码。~~ → ✅ 已删除，由统一 `ConversionPlanner` 取代。

## 本轮执行顺序

阶段 01–10 的计划内主流程与 Release 文档/产物收尾已落地（见 `docs/phase12-release.md`）。
当前顺序为：提交并发布双 Edition GitHub Release；Office 后续继续扩展像素级与跨设备矩阵。
